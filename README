# File Transfer Protocol
This software implements the FTP protocol for file transfer supporting `GET`, `PUT`, `OPEN`, `CLOSE`, and `QUIT` operations.
Files can be downloaded from `/server` file using `GET` and can be pushed to `/server` from `/client` using `PUT`.
